# 编译型号（产品线 → 型号列表）。型号 id → (build 脚本, 产品参数)。
# 分支所属产品线 = branch.md section（同源组，人工标注）。产品线未配置时任务报错停止，
# 不做静默兜底（避免用错编译脚本）。
build_types:
  # FTTR-B 系列
  "2600m":
    script: "RTL9617C_build_ci.sh"
    product: "2600m"

build_models_by_section:
  # section 标题子串匹配（容忍 "1.1 " 编号前缀）
  "4.34 主分支":
    - "2600m"
