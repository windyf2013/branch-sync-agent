# web-platform Specification (delta)

## MODIFIED Requirements

### Requirement: 实时工作台首页

首页为任务中心，按分支粒度分区展示最近一个周期内的所有任务，所有操作在任务详情页完成。

#### Scenario: 工作台待办与总览

- **当** 操作者/查看者进入首页
- **则** 首页分两区块：自动（Agent cron 周期展开的分支任务）与手动（用户发起的分支任务）
- **且** 任务按分支粒度展示为面板列表，含状态徽章（进行中/排队/成功/失败/停批/已放弃）
- **且** 仅列出最近一个周期窗口内的任务；超出窗口的历史收敛进历史报告页
- **且** 展示 Agent 运行状态（V1 周期运行中/失败醒目标记）
- **且** 运行中周期仅显示"进行中"徽章，不展示未完成周期的详情

#### Scenario: 页面状态实时性

- **当** 用户长时间停留后操作
- **则** 每次请求从 V1 投影读取最新状态，不缓存过期数据

#### Scenario: 任务详情页承载操作

- **当** 用户点击某分支任务
- **则** 跳转任务详情页，所有操作在该页完成：查看现场证据、WebSSH（失败/停批且现场存在）、重跑、推送（SUCCESS）、放弃/恢复、人工项处理
- **且** 任务详情页复用现有分支/commit 详情渲染，patch/编译日志按需加载

### Requirement: 手动触发同步与重跑

平台提供引导式同步发起（B 区）与任务重跑，用户手动发起视为人工审核通过、无需四态决策。

#### Scenario: 触发同步

- **当** 操作者选择源分支
- **则** 平台经 `bsa commits <src>` 加载候选 commit 列表（sha + 提交说明 + 时间，最近 N 条平铺）
- **且** 操作者多选勾选候选 commit 并选择目标分支后发起
- **且** 用户手动发起 = 人工审核通过，不做四态结论判定，直接同步
- **且** 已包含于目标分支的 commit 同步时按 EMPTY 跳过并在结果体现

#### Scenario: 发起后自动衔接

- **当** 操作者提交新建同步
- **则** web 等待后台"发起成功"反馈后自动刷新，任务进入任务中心手动区块
- **且** 任务状态机 queued→running→succeeded/failed，失败含错误信息，可重试

#### Scenario: 排队与并发

- **当** V1 周期运行中用户发起同步/重跑
- **则** 任务进入 queued 状态并显示"排队中（等待当前周期完成）"
- **且** 同一目标分支已有运行/排队任务时拒绝重复触发
- **且** 全局 flock 串行化所有 V1 执行，等待超时后任务失败并提示重试

#### Scenario: 重跑

- **当** 操作者对失败/停批分支重跑
- **则** 瞬态失败可直接重跑
- **且** 需人工干预的任务先经 WebSSH 在线处理现场，再保留现场续跑（resume 验证→更新 patch）
- **且** 支持 `--fresh` 基于当前远端重建重同步（先重判结论，已合入/超范围则停止提示）
- **且** 超期分支仅可只读查看与重新同步，不提供续做/推送入口

### Requirement: 人工项处理

平台提供失败现场查看与人工决策能力，失败/停批任务可经受限 WebSSH 在线处理。

#### Scenario: 查看失败现场

- **当** 存在停批/失败分支
- **则** 展示冲突现场、编译错误、Agent 修复记录（来自投影与日志文件）

#### Scenario: WebSSH 在线处理

- **当** 任务为失败/停批且有 worktree 现场
- **则** 任务详情页提供受限 WebSSH 终端，映射到该任务 worktree
- **且** 终端仅限该 worktree 目录、禁 sudo、禁出目录（三层防御 + 部署约束）
- **且** 仅"有现场且失败/停批"的必要任务开放；ManualReview 待确认项不走 SSH
- **且** 终端操作审计（会话级必记 + 命令脱敏记录）

#### Scenario: 改判定

- **当** 操作者修改某 commit 的 is_bug_fix / risk 判定
- **则** 平台经 V1 CLI 写入 judgments.json（人工覆盖优先）
- **且** 生效于下一次决策执行（cron 周期或重跑重判），不追溯改写已冻结的 decisions.json
- **且** 提供"立即重跑"入口让新判定尽快生效
- **且** 改判定行为记录操作日志（操作人/时间/sha/新值）

#### Scenario: 确认继续与放弃

- **当** 操作者确认某 ManualReview 项允许同步
- **则** 复用直同步路径（指定 sha 强制同步）执行
- **且** 操作者可放弃某 commit/分支，标记并记录操作日志

### Requirement: 推送操作

平台对任何已验证成功（SUCCESS）现场开放推送，键在任务详情页。

#### Scenario: 推送前置条件

- **当** 操作者在任务详情页点推送
- **则** 平台校验四道闸，任一不过则拒绝并显示原因：
  1. 分支状态为 SUCCESS（全部 cherry-pick + 全部 required_models 编译通过）
  2. worktree 存在
  3. 目标分支不在 safety_rules 的 forbidden_branches
  4. git status --porcelain 干净
- **且** 推送键对自动（周期）与手动（手动同步/重跑成功）的所有 SUCCESS 现场开放
- **且** 仅当前周期活 worktree 可推送；超期分支无推送入口

#### Scenario: 推送执行

- **当** 四道闸通过且操作者二次确认（确认框展示目标分支、commit 范围、patch 摘要）
- **则** 平台在对应 worktree 执行 `git push origin HEAD:<target>`
- **且** 禁止 `--force`（硬约束）；远端非 fast-forward 时 git 拒绝，平台回显"远端已前进，请重新同步"
- **且** 推送结果回显（成功/失败+错误），失败可重试（重试=重新同步）
- **且** 推送记录操作日志（操作人、时间、分支、commit 范围、结果）

#### Scenario: 无自动推送

- **当** 平台运行
- **则** 平台无任何自动推送逻辑，推送必须由操作者人工触发并确认
- **且** Agent 侧不新增推送白名单，推送仅存在于平台受限执行器

## ADDED Requirements

### Requirement: 放弃/恢复

平台持久化放弃标记，任务列表联动过滤，支持恢复。

#### Scenario: 放弃与恢复

- **当** 操作者对某分支任务或 commit 标记放弃
- **则** 平台本地持久化放弃状态（跨周期有效）
- **且** 任务中心/待处理列表自动过滤已放弃项，不再重复提示
- **且** 操作者可恢复已放弃项，恢复后重新进入任务流程
- **且** 放弃/恢复行为记录操作日志


### Requirement: 新建同步候选 commit 数据源

平台经 V1 只读 CLI 获取源分支候选 commit 列表，不直接读取 git。

#### Scenario: 加载候选 commit

- **当** 操作者在新建同步中选择源分支
- **则** 平台调用 `bsa commits <src>` 获取候选 commit 列表（sha + 提交说明 + 时间，最近 N 条平铺）
- **且** 列表仅作展示供勾选，不做已同步/已包含过滤
- **且** 加载失败时提示"候选 commit 加载失败"，不阻塞其他操作
